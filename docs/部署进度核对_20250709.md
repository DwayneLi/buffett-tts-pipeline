# 部署进度核对报告 — buffett-tts-pipeline

> 核对日期：2025-07-09
> 核对人：WorkBuddy（基于已 clone 仓库）
> 对照基准：`docs/PRD.md`、`docs/部署指南.md`、`docs/架构设计.md`、`docs/技术验证报告.md`、README 状态清单、三份脚本源码

---

## 一、一句话结论

**设计 / 编码阶段 100% 完成，实体环境部署 0% 开始。**

仓库里需求、PRD、技术验证、架构、核心脚本都已齐备（README 的"当前状态"清单前 5 项全打勾）。但对照 `部署指南` 的 6 步，**只有第 4、5 步的代码写好了，第 1、2、3、6 步要么纯物理操作从未执行、要么只停在文档**。我们目前卡在 **"代码写完 ↔ 第一次真机跑通"** 的交界点上。

> ⚠️ 沙箱环境是 Ubuntu，没有你的 Mac / Windows+4070S 真机，也拿不到局域网，所以**实体部署动作必须由你在自己硬件上执行**；我这边能做的是代码评审、补缺口、写核对清单，把"真机第一次跑通"的障碍提前扫清。

---

## 二、部署六步对照表

| 部署步骤（来自部署指南） | 状态 | 现状说明 |
|---|---|---|
| 1. Windows 环境搭建（CUDA / PyTorch / GPT-SoVITS / 预训练模型）| ⬜ 未开始 | 纯物理操作，需在你的 Windows 主机执行，无代码可验证 |
| 2. 收集巴菲特音频素材（Mac：yt-dlp 下载 + 裁剪）| ⬜ 未开始 | 需你的 Mac + 网络，依赖步骤 1 的参考音频处理 |
| 3. 音色克隆训练验证（Windows：零样本 / few-shot）| ⬜ 未验证 | 强依赖步骤 1，当前无法判定音色效果 |
| 4. 翻译 + 背景增强流水线 | 🟡 代码完成、未实测 | `translate.py` 已写，但缺 `LLM_API_KEY` 从未真跑 |
| 5. 端到端串联脚本 | 🟡 代码完成、未实测 | `pipeline.py` 已写，强依赖 Windows TTS 服务在线 |
| 6. 推送到手机 | ⬜ 未脚本化 | 仅文档指南（HTTP / AirDrop / iCloud），无代码 |

**进度条**：文档与代码 ≈ 100% ｜ 实体部署 ≈ 0%

---

## 三、代码就绪度检查

| 检查项 | 结果 |
|---|---|
| `scripts/pipeline.py` 语法 | ✅ 通过 |
| `scripts/translate.py` 语法 | ✅ 通过 |
| `scripts/windows_tts_server_guide.py` 语法 | ❌ **SyntaxError**（docstring 含 `C:\Users\…` 的 `\U` 被当成 unicode 转义，需用 `r"""` 原始字符串修复）|
| `requests` 依赖 | ✅ 可用 |
| `ffmpeg` | ✅ 沙箱已有（Mac / Windows 端也需自备）|

### 已正确实现的 PRD 条目
- **F2-02** 长文（>4000 字）自动分块翻译 ✅
- **F2-05 / F6-04** 翻译缓存询问复用、合成断点续传（跳过已存在文件）✅
- **F3** 背景增强（通过 system prompt 注入 `【📌 背景】`）✅
- **F4** 按 `---` 分段、角色标注 ✅
- **F6-03** 单段合成超时（300s）跳过并继续 ✅
- **F7** ffmpeg 拼接 + 每段 1.5s 静音间隔 ✅
- **F9-03** Windows TTS 不可达时报告并暂停 ✅

---

## 四、PRD 缺口（影响验收，需补齐）

| 编号 | 缺口 | 严重度 | 对验收的影响 | 建议 |
|---|---|---|---|---|
| **F2-04** | `translate.py` **无 LLM 重试**（3 次 + 指数退避）| 🔴 高 | 任一 chunk 超时即整段失败，不符 AC-01 | 立即补 `tenacity` 式重试 |
| **F6 健康检查** | `pipeline.py` 探测 `GET /status`，但 GPT-SoVITS `api_v2.py` **默认无 `/status` 端点** | 🟠 中 | 探测恒返回 404，仅告警不阻断，但检查结果失真、易误判 | 改为探测真实端点（`/docs` 或 `/tts` 轻量请求）或直接去掉 |
| **F1-02** | 无 URL 抓取，仅支持本地文件输入 | 🟠 中 | 不影响首版，但偏离 PRD 可选能力 | 后续迭代，本期可接受 |
| **windows_tts_server_guide.py** | 文件本身无法运行（SyntaxError）| 🟠 中 | 指南脚本打不开，新人易踩坑 | 改 `r"""` 或降级为纯 `.md` |
| **F8 音色调参** | 仅文档，无代码（pitch shift / speed）| 🟢 低 | 非 P0，后续迭代 | v1.1 处理 |
| **F10 推送手机** | 仅文档，无脚本 | 🟢 低 | 不影响产出，手动可替代 | 后续迭代 |

---

## 五、沙箱能做 vs 必须由你来做

**我可以在此环境直接推进的（无需你的硬件）：**
1. 修 `pipeline.py` 的 `/status` 健康检查端点（否则真机第一步排查会被误导）
2. 给 `translate.py` 补 F2-04 重试逻辑（PRD 硬性要求，直接卡验收）
3. 修 `windows_tts_server_guide.py` 的 SyntaxError
4. 可选：补 F1-02 URL 抓取、F10 推送脚本
5. 写 `config/config.json`（基于 `config.example.json` 填好注释）与一份"真机首次跑通 checklist"

**必须由你在真机上执行的（需要硬件 / 账号 / 局域网）：**
- Windows 装 CUDA + PyTorch + GPT-SoVITS + 预训练模型
- Mac 用 yt-dlp 收集并裁剪巴菲特参考音频
- 跑零样本克隆，主观验证巴菲特音色相似度（AC-03）
- 填 `LLM_API_KEY`、启动 Windows TTS 服务，端到端跑 `pipeline.py`（AC-05）
- 参数调优（speed_factor / top_k 等，对应 F8 / NFR）

---

## 六、建议的下一步（按价值排序）

1. **【高】** 修 `pipeline.py` 健康检查 + 补 `translate.py` 重试 —— 两个会卡真机首跑的 PRD 缺口，我先改。
2. **【高】** 你侧启动 Windows GPT-SoVITS `api_v2.py`、填 `config.json`、设 `LLM_API_KEY`，用一段 5s 巴菲特音频做**第一次零样本克隆验证**（AC-03）。
3. **【中】** 准备一段 2024/2025 股东会英文 transcript，先单独跑 `translate.py` 验证翻译 + 背景增强质量（AC-01 / AC-02）。
4. **【中】** 修 `windows_tts_server_guide.py` 语法错误。
5. **【低】** 补 F1-02 / F10，进入 v1.1 迭代。

---

## 七、待你确认的关键决策（来自 PRD §九）

| 编号 | 问题 | 当前默认 | 状态 |
|---|---|---|---|
| Q-01 | 翻译用 DeepSeek 还是 OpenAI？| DeepSeek（便宜）| 待你拍板 |
| Q-02 | 先处理哪一年股东会？| 2025（最新）| 待你拍板 |
| Q-04 | 音色年轻化/清晰化程度？| 未定（speed 0.9）| 实测后定 |
| Q-05 | 是否需要 Web UI？| 本期命令行 | 已定（后续 Gradio）|
